<h1> My Tasks </h1>

<ul>
  <% @tasks.each do |task| %>
    <li>
      Titulo: <%= link_to task.title, task_path(task) %>
      Detalhes: <%= task.details %>

      |
      <%= link_to "Delete",
                  task_path(task),
                  data: { turbo_method: :delete, turbo_confirm: "Are you sure?" } %>
    </li>
  <% end %>
</ul>
